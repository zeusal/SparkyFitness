/**
 * Stub for `remark-gfm`, wired in through `moduleNameMapper`.
 *
 * ESM-only like `react-markdown` itself (see `./reactMarkdown.tsx`). The
 * react-markdown stub ignores its plugins entirely, so this only has to be a
 * valid, importable no-op.
 */
export default function remarkGfm(): undefined {
  return undefined;
}
