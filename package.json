{
  "packageManager": "pnpm@10.33.4",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.0.0",
    "lint-staged": "^16.4.0",
    "prettier": "^3.8.1"
  },
  "lint-staged": {
    "SparkyFitnessFrontend/**/*.{ts,tsx}": [
      "npx prettier --write"
    ],
    "SparkyFitnessServer/**/*.{js,ts}": [
      "npx prettier --write"
    ]
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  }
}
